<Window
    x:Class="TODO_Desktop.Views.LoginWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Login / Register"
    Height="520" Width="380"
    WindowStartupLocation="CenterScreen"
    ResizeMode="NoResize"
    Background="{DynamicResource WindowBackgroundBrush}">
    <Border Margin="16"
            CornerRadius="12"
            Background="{DynamicResource PanelBackgroundBrush}"
            BorderBrush="{DynamicResource PanelBorderBrush}"
            BorderThickness="1">
        <StackPanel Margin="20">
            <TextBlock
                Text="Welcome to TODO-list! Please log in or register."
                FontWeight="SemiBold"
                FontSize="16"
                Foreground="{DynamicResource TextColorBrush}"
                Margin="0,0,0,12"
                TextWrapping="Wrap"/>
            
            <!-- Login Form -->
            <StackPanel x:Name="LoginPanel" Visibility="Visible">
                <Label Content="Email or Username" />
                <TextBox x:Name="LoginEmailBox" Margin="0,0,0,8" Height="28"/>
                <Label Content="Password" />
                <PasswordBox x:Name="LoginPasswordBox" Margin="0,0,0,12" Height="28"/>
                <Button
                    Content="Login"
                    Click="Login_Click"
                    Height="32"
                    Background="#3B82F6"
                    Foreground="White"
                    BorderBrush="#3B82F6"
                    FontWeight="Bold"/>
                <TextBlock Margin="0,10,0,0" HorizontalAlignment="Center">
                    <Run Text="Don't have an account? "/>
                    <Hyperlink Click="RegisterLink_Click">Register</Hyperlink>
                </TextBlock>
            </StackPanel>
            
            <!-- Register Form -->
            <StackPanel x:Name="RegisterPanel" Visibility="Collapsed">
                <Label Content="Username" />
                <TextBox x:Name="RegUsernameBox" Margin="0,0,0,8" Height="28"/>
                <Label Content="Email" />
                <TextBox x:Name="RegEmailBox" Margin="0,0,0,8" Height="28"/>
                <Label Content="Password" />
                <PasswordBox x:Name="RegPasswordBox" Margin="0,0,0,8" Height="28"/>
                <Label Content="Confirm Password" />
                <PasswordBox x:Name="RegConfirmPasswordBox" Margin="0,0,0,12" Height="28"/>
                <Button
                    Content="Register"
                    Click="Register_Click"
                    Height="32"
                    Background="#10B981"
                    Foreground="White"
                    BorderBrush="#10B981"
                    FontWeight="Bold"/>
                <TextBlock Margin="0,10,0,0" HorizontalAlignment="Center">
                    <Run Text="Already have an account? "/>
                    <Hyperlink Click="LoginLink_Click">Login</Hyperlink>
                </TextBlock>
            </StackPanel>
        </StackPanel>
    </Border>
</Window>