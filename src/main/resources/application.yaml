spring:
  application:
    name: todo

  datasource:
    url: jdbc:postgresql://localhost:5432/todo
    username: postgres
    password: ${DB_PASSWORD}
    driver-class-name: org.postgresql.Driver
  exposed:
    generate-ddl: true